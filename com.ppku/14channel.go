package main

func main() {
	/*
			通道的声明
			var chanName chan channelType
		 	var ch chan string
			ch = make(chan string)
			ch:=make(chan string)

			发送数据
			channel <-data
			接受数据
			value:= <-channel
	*/

}
